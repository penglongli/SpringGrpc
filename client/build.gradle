group 'com.test.client'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile project(":lib-proto")

    compile 'org.springframework.boot:spring-boot-starter-web:2.0.2.RELEASE'
}
